const AuthenticationError = require('./detail/authentication-error')
const AccessDeniedError = require('./detail/access-denied-error')

module.exports = {
    AccessDeniedError,
    AuthenticationError    
}